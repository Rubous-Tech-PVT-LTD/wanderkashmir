import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const properties = await prisma.property.findMany({ where: { isApproved: true, status: 'APPROVED' }, select: { id: true, name: true } }); properties.forEach(p => console.log('- **' + p.name + '**: https://www.wanderkashmir.com/stays/' + p.id)); } main().finally(() => prisma.$disconnect());
